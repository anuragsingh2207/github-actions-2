name: Receiver
 
on:
  repository_dispatch:
 
jobs:
  receiver:
    name: Receive
    runs-on: ubuntu-latest
    steps:
      - name: A event received
        run: |
          echo "payload Foo '${{ github.event.client_payload.foo }}'"